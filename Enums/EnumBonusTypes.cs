
namespace ConsoleApp1.Enums
{
    public enum EnumBonusTypes
    {
        FreeBet = 1,
        AccaInsurance = 2
    }

    //if (Enum.IsDefined(typeof(EnumBonusTypes), s.Trim()))
    //{
    //    Enum.TryParse(s.Trim(), out EnumBonusTypes BCAType);
    //    BCAType = (EnumBonusTypes)Enum.Parse(typeof(EnumBonusTypes), s.Trim());
    //}
}
